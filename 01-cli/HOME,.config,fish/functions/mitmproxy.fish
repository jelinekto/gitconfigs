# Defined via `source`
function mitmproxy --description 'alias mitmproxy mitmproxy --set confdir /home/jeli/.config/mitmproxy'
 command mitmproxy --set confdir /home/jeli/.config/mitmproxy $argv; 
end
